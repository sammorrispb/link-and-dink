# Supabase — link-and-dink project (newsletter signups)
# Server-side only. The publishable key is safe to expose, but writes are
# gated by an INSERT-only RLS policy on the `subscribers` table.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
